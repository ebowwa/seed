{
  "name": "@codespaces/node-agent",
  "version": "1.0.0",
  "description": "PM Daemon Agent - manages Ralph loops and provides oversight APIs",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun --hot run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist",
    "test": "bun test"
  },
  "dependencies": {
    "@codespaces/ai": "workspace:*",
    "@codespaces/claudecodesdk": "workspace:*",
    "@codespaces/schemas": "workspace:*",
    "@codespaces/tailscale": "workspace:*",
    "@codespaces/terminal": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
