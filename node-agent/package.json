{
  "name": "seed-node-agent",
  "version": "0.1.0",
  "description": "Node Agent for Ralph Loop orchestration on VPS nodes",
  "type": "module",
  "scripts": {
    "dev": "bun --hot run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
